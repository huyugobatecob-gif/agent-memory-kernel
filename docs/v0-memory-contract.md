# v0 Memory Contract

This document defines the stable contract for `v0.1.x`.

## Goals

The kernel should let an agent answer three questions before using memory:

1. What did we remember?
2. Where did it come from?
3. Why should I trust it?

The answer must be machine-readable enough for agents and readable enough for
humans.

## Lifecycle

### 1. Event

An event is an append-only raw record.

Examples:

- user note;
- session summary;
- tool result;
- project decision;
- failed attempt;
- successful attempt.

Fields:

- `event_id`
- `created_at`
- `actor`
- `scope`
- `source_type`
- `source_ref`
- `content`
- `metadata_json`

### 2. Candidate Memory

A candidate is a proposed durable memory extracted from an event.

Candidate statuses:

- `pending`: needs review;
- `approved`: promoted to active memory;
- `rejected`: explicitly not useful or incorrect;
- `quarantined`: contains secret-like or unsafe content.

Fields:

- `candidate_id`
- `event_id`
- `proposed_text`
- `kind`
- `scope`
- `confidence`
- `sensitivity`
- `source_trust`
- `status`
- `reason`
- `extraction_json`

### 3. Active Memory

An active memory is allowed to be retrieved by agents.

Fields:

- `memory_id`
- `candidate_id`
- `created_at`
- `updated_at`
- `text`
- `kind`
- `scope`
- `confidence`
- `sensitivity`
- `source_trust`
- `status`
- `expires_at`

Active memory can be corrected or soft-deleted. Deletion should not erase audit
history.

## Default Scopes

- `personal`: user preferences and personal context.
- `professional`: work context, projects, decisions, rules, lessons.
- `project`: optional project-specific context.
- `agent`: operational memory for a specific agent.
- `session`: short-lived context that may later be summarized.

The public starter template should keep `personal` and `professional` as the
two primary columns. Domain users can add project-specific lanes.

## Memory Kinds

Recommended starter kinds:

- `fact`
- `preference`
- `rule`
- `decision`
- `gotcha`
- `pattern`
- `attempt`
- `outcome`

The current deterministic extractor emits a conservative subset:

- `fact`
- `preference`
- `rule`
- `decision`
- `gotcha`

## Trust

`source_trust` is not the same as truth. It only tells the agent how cautious to
be with the source.

Values:

- `trusted`: manually provided by a user or trusted profile.
- `system`: generated by a controlled system process.
- `user`: reserved for direct user-owned identity flows.
- `untrusted`: external tools, web pages, logs, scraped text, model output.

Default policy:

- trusted/manual notes may be auto-approved when explicitly requested;
- untrusted sources stay pending;
- secret-like values are quarantined.

## Sensitivity

Values:

- `public`
- `internal`
- `personal`
- `secret`

Secret-like content should not become active memory. It should go to quarantine
for explicit handling.

## Graph Model

The graph is intentionally small in v0.

Node types:

- `memory`
- `person`
- `project`
- `document`
- `tool`
- `decision`
- `preference`
- `rule`

Edge types:

- `relates_to`
- `belongs_to`
- `uses`
- `decided_in`
- `derived_from`
- `supersedes`
- `conflicts_with`

The v0 store creates an anchor `memory` node and links extracted nodes to it
with `relates_to`. More advanced extractors can add richer graph structures.

## Context Pack

A context pack is the agent-facing output format.

It must include:

- selected memory text;
- scope;
- kind;
- confidence;
- source;
- trust;
- why selected;
- why trusted or why to verify.

Example:

```markdown
## Agent Memory Context

Selected memories:
- [professional:rule:medium] Rule: record failed SEO loop attempts.
  (source=evt_...; trust=trusted; why_selected=query match;
  why_trusted=trusted source with medium confidence)
```

## Corrections

Corrections should update active memory text, not mutate raw source events.

When a memory becomes wrong:

1. Correct it if the new statement is a replacement.
2. Soft-delete it if it should no longer be retrieved.
3. Add a new event if the correction itself is important context.

## Security Notes

Memory is an agent control surface. Treat it as part of the prompt boundary.

Required safeguards:

- no active memory without provenance;
- quarantine secret-like text;
- keep untrusted external content pending;
- support correction and deletion;
- include trust metadata in every context pack;
- do not let external content silently rewrite durable rules.
